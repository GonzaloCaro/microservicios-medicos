# microservicios

### run

`mvn spring-boot:run`

### Limpiar compilación previa

`mvn clean`

### Limpiar + compilar

`mvn clean install`

### instale dependencias + compile:

`mvn clean package`

### Correr Test
`mvn clean verify`